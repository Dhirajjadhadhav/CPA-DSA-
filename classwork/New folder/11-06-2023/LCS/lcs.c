#include <stdio.h>
#include <stdlib.h>


#define C(i, j) (*(c + (i) *n + (j)))
#define B(i, j) (*)



void lcs(const char *x,const char *y, int *C, int *B)
{
    int n = strlen(x);
    int m = strlen(y);

    //CORMEN : allocate c[0 .. m, 0...n]
    y = 
}